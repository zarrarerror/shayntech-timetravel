version: '3.8'
services:
  timetravel:
    build:
      context: ..
      dockerfile: enterprise/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - tt_data:/data
    environment:
      - TT_HOST=0.0.0.0
      - TT_PORT=8080

volumes:
  tt_data:
